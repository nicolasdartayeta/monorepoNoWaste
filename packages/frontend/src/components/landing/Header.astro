---
import NoWasteLogo from '@/components/NoWasteLogo.astro';
import { buttonVariants } from "@/components/ui/button"

---

<haeder>
    <NoWasteLogo class='w-full h-auto mt-10'/>
            
    <h4 class='text-textColor flex justify-center font-semibold my-2'>Conectando negocios y personas</h4>
    <nav class="uppercase w-full grid grid-cols-1 md:grid-cols-3 gap-2 my-10">
        <a class={buttonVariants({ variant: "outline" }) + " w-full text-textColor bg-transparent border border-textColor hover:bg-textColor hover:text-white"} href='/'>proyecto</a>
        <a class={buttonVariants({ variant: "outline" }) + " w-full text-textColor bg-transparent border border-textColor hover:bg-textColor hover:text-white"} href='/about'>sobre nosotros</a>
        <a class={buttonVariants({ variant: "outline" }) + " w-full text-textColor bg-transparent border border-textColor hover:bg-textColor hover:text-white"} href='/about'>a confirmar</a>
    </nav>
</haeder>